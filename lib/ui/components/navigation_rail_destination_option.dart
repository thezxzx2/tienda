import 'package:flutter/material.dart';

class NavigationRailDestinationOption extends StatelessWidget {
  final Icon icon;
  final String label;

  const NavigationRailDestinationOption({
    super.key,
    required this.icon,
    required this.label,
  });

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
